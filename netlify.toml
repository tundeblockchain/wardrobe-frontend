[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback so client-side routes resolve on refresh and deep links.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
